1. 能够胜任对话（session）内记忆和跨 session 记忆系统
2. 能够实现固定信息和随时间变化的信息的记忆。如用户的基础信息，需求的变化，喜好的变化，工作岗位和薪资的变化等。
3. 时变的信息需要能够检索其对应的历史。
4. 一套基于FastAPI + Python Backend + TS + Vite + Shadcn-UI + Lucide + ai.sdk 的前后端分离的 Chatbot。需要能够监控 Memory 的积累情况和事件图的执行进度。
5. （基本）时不变且大段的内容，不断从 memory 中分离出来形成新的笔记。
6. Chatbot 可以根据事件图逐步执行一个大型任务。事件图同样也可以由Agent 实现，并在执行过程中逐渐调整。
7. Chatbot 有一套可以访问 memory 和 event 图的 MCP，实现 CRUD。